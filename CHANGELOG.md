# 更新紀錄

## [未標記版本] - 2025-02-07

### 新增
- **全站登出按鈕**：每個頁面（除登入頁外）header 右上角均顯示使用者資訊與登出按鈕，方便隨時登出
- **手機版響應式版面**：手機螢幕下登出按鈕不再與網站標題重疊，改為在導覽列下方顯示並置中

### 修正
- **收支管理 Excel 匯出**：將原本 4 個金額欄位（金額1～4）改為單一「金額」欄位，與資料庫結構一致，匯出內容更精簡易讀

### 技術說明
- 所有含 header 的頁面均新增 `user-info` 區塊（`currentUser` 與登出按鈕）
- 手機版（`@media (max-width: 768px)`）下 `.user-info` 使用 `position: static` 避免與標題重疊
- Excel 匯出 API (`GET /api/transactions/export`) 欄位：日期、說明、金額、類型、類別、公司名稱、帳戶名稱、帳號、備註
